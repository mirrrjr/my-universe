### 📌 Dars maqsadi

O‘quvchilar JavaScript yordamida web sahifalarga **mantiq**, **hisob-kitob**, va **dinamik xatti-harakatlar** qo‘shishni o‘rganadilar.

---

## 🧠 JavaScript nima?

**JavaScript (JS)** — brauzerda ishlaydigan dasturlash tili bo‘lib:

- Foydalanuvchi bilan interaktivlik yaratadi
    
- Frontend’ga “jon” kiritadi
    
- Backend (Node.js) va mobil sohalarda ham qo‘llanadi
    

---

## ⚙️ JavaScript ulash usullari

### External JS (tavsiya etiladi)

```html
<script src="app.js"></script>
```

📌 Script odatda `</body>` dan oldin ulanadi.

---

## 🧱 Asosiy tushunchalar

### O‘zgaruvchilar

```js
let name = "Ali";
const age = 20;
var city = "Tashkent"; // tavsiya etilmaydi
```

- `let` — o‘zgaruvchan
    
- `const` — o‘zgarmas
    

---

### Ma’lumot turlari

- `string`
    
- `number`
    
- `boolean`
    
- `null`
    
- `undefined`
    
- `object`
    

```js
let isActive = true;
let user = { name: "Ali", age: 20 };
```

---

## 🔀 Operatorlar

- Arifmetik: `+ - * / %`
    
- Taqqoslash: `==`, `===`, `!=`, `>`
    
- Mantiqiy: `&&`, `||`, `!`
    

---

## 🔁 Shartlar (Conditions)

```js
if (age >= 18) {
  console.log("Ruxsat berildi");
} else {
  console.log("Ruxsat yo‘q");
}
```

---

## 🔄 Looplar

### For loop

```js
for (let i = 0; i < 5; i++) {
  console.log(i);
}
```

### While loop

```js
let i = 0;
while (i < 5) {
  i++;
}
```

---

## 🧩 Functionlar

```js
function greet(name) {
  return "Salom " + name;
}
```

Arrow function:

```js
const sum = (a, b) => a + b;
```

---

## 📦 Array va Object

### Array

```js
let fruits = ["apple", "banana", "orange"];
fruits.push("pear");
```

### Object

```js
let user = {
  name: "Ali",
  age: 22
};
```

---

## 📂 Tavsiya etilgan fayl tuzilmasi

```text
03-javascript/
└── 01-js-basics/
    ├── index.html
    ├── app.js
    └── README.md
```

---

## 🔨 Amaliy mashg‘ulot

### Topshiriqlar:

1. JS fayl ulash
    
2. O‘zgaruvchilar bilan ishlash
    
3. Shart va loop yozish
    
4. Function yaratish
    
5. Array ustida amal bajarish
    

🎯 **Mini loyiha:**

> Oddiy kalkulyator (console orqali)

---

## 🧪 Baholash mezonlari

- `let` va `const` to‘g‘ri ishlatilgan
    
- Kod tushunarli
    
- Mantiq to‘g‘ri ishlaydi
    
- Console’da xatosiz ishlaydi
    

---

## 📌 GitHub talablari

- `app.js` alohida fayl
    
- README.md tushuntirish bilan
    
- Kamida 5 ta mazmunli commit
    
- Aniq commit message
    

